{% extends 'base.html.twig' %}

{% block body %}
    <h1>Organisateur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organisateur.id }}</td>
            </tr>
            <tr>
                <th>Nomsociete</th>
                <td>{{ organisateur.nomSociete }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ organisateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ organisateur.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ organisateur.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ organisateur.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('organisateur_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('organisateur_edit', { 'id': organisateur.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
