{% extends "base.html.twig" %}

{% block title %}AppBundle:Admin:delete{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-sm-6 col-sm-push-3 col-xs-12">
        	<h1 class="h1">Utilisateur désactivé avec succées</h1>
        	</br>
        	<h3 class="h3">Désactivé par erreur ?</h3>
        	<a class="btn btn-sm btn-success" href="{{ path('activate_abonne', { 'id': abonne.id }) }}" role="button">
            	<span class="glyphicon glyphicon-ok"></span>
            	Activer
            </a>
            </br>
            </br>
			<a href="{{ path('admin_users') }}" class="btn btn-primary" role="button">
            	<span class="ion-reply"></span>
            	Retour à la liste
            </a>
		</div>
	</div>
{% endblock %}
