
<!-- start header -->
<header>
    <div class="navbar navbar-default navbar-static-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" style="margin-left: -10px;" href="{{ path('homepage') }}"><span>T</span>unisia<span>E</span>vents</a>
            </div>
            <div class="navbar-collapse collapse ">
                <ul class="nav navbar-nav">
                    <li {{ (app.request.attributes.get('_route') == 'homepage') ? ' class="active"' }} >
                        <a href="{{ path('homepage') }}">Accueil</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li {{ (app.request.attributes.get('_route') == 'admin_index') ? ' class="active"' }} >
                            <a href="{{ path('admin_index') }}">Admin</a>
                        </li>
                    {% endif %}
                    <li {{ (app.request.attributes.get('_route') == 'event_new') ? ' class="active"' }} >
                        <a href="{{ path('event_new') }}">Organiser votre Event</a>
                    </li>
                    {% if app.user %}
                        <li {{ (app.request.attributes.get('_route') == 'mes_events') ? ' class="active"' }} >
                            <a href="{{ path('mes_events') }}">Mes Events</a>
                        </li>
                <!--        <li>
                            <a href="#">Mon Compte</a>
                        </li>
                -->
                        <li>
                            <a href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
                        </li>
                    {% else %}
                        <li {{ (app.request.attributes.get('_route') == 'inscription') ? ' class="active"' }} >
                            <a href="{{ path('inscription') }}">Inscription</a>
                        </li>
                        <li {{ (app.request.attributes.get('_route') == 'fos_user_security_login') ? ' class="active"' }} >
                            <a href="{{ path('fos_user_security_login') }}">Connexion</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</header>
<!-- end header -->
